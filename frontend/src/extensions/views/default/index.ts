import type { ViewPlugin } from '../registry';

const plugin: ViewPlugin = {
  id: 'default',
  label: 'Default',
  description: 'Standard modern tree view',
  category: 'builtin',
};

export default plugin;
